modules = ["nodejs-20", "web"]

run = "cd bd-platform && npm install && npm run dashboard"
entrypoint = "bd-platform/src/server/index.ts"

[nix]
channel = "stable-25_05"

[agent]
expertMode = true

[[ports]]
localPort = 3000
externalPort = 80
